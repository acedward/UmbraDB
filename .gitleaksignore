# Exact-fingerprint suppressions for `gitleaks detect` (format: commit:path:rule:line).
#
# These are the ONLY suppressed findings. G19 deliberately performs no git-history rewrite, so the
# valueless Preview testnet wallet secret committed before it was untracked recurs on every
# full-history scan. Pinning fingerprints suppresses precisely those occurrences: a different
# secret, the same file at a different commit, or either field in the working tree still fails.
#
# Both lines of the historical file are listed because the custom `umbradb-wallet-seed-hex` rule
# catches BOTH the seed (line 4) and the derived night secret key (line 5). The stock
# undetected until that rule was added.
#
# The overlapping generic-api-key fingerprint was REMOVED: the custom rule takes precedence over
# it, so that entry was never consulted -- verified by deleting it and re-scanning clean.
#
# Verified: with these two entries, a full-history scan of 207 commits reports no leaks, while
# planting a fresh hex value in EITHER field of the tracked .example template fails the gate.
be646fcff58052a77e848914a79ccef2279cf78f:nix/midnight-env/test-wallets/preview-test-wallet.json:umbradb-wallet-seed-hex:4
be646fcff58052a77e848914a79ccef2279cf78f:nix/midnight-env/test-wallets/preview-test-wallet.json:umbradb-wallet-seed-hex:5

# Commit e5f53f5 put the official method names `eth_createAccessList` and `eth_simulateV1`
# next to each other. The stock `generic-api-key` rule treated the `AccessList` name as key
# context and the following method name as a high-entropy credential value. Both are public
# Ethereum JSON-RPC method names, not credentials, so suppress only that historical fingerprint.
# The current list separates the literals and leaves `eth_createAccessList` last in its group to
# avoid creating the same adjacency when this test is next changed.
e5f53f5122c973556531ce07c696a32e9f911b5f:evm-rpc/test/not-implemented.test.ts:generic-api-key:31

# Projects 00020/00021 (token indexer). Commits 4e06464 and 7b03bc4 recorded real Stagenet colour
# vectors: `registryTokenId`, `observedUtxoTokenType` and `tokenType` fields (and one doc comment
# in color.ts) holding 64-hex TOKEN COLOURS. A colour is public chain data -- it is printed on
# every UTXO and served by this indexer's own API -- but the stock `generic-api-key` rule reads
# the word "token" in the field name as credential context and the hex as a high-entropy value.
# Suppressed by exact fingerprint, as above: a new vector line, or a real key pasted into any of
# these files, is a different fingerprint and still fails the gate.
7b03bc4cb9f0e1448860ca9dfebdc6951f1b6a57:token-indexer/color.ts:generic-api-key:34
4e06464af1cee9149d5bf8b62025fdb45a83465f:token-indexer/test/fixtures/color-vectors.json:generic-api-key:17
4e06464af1cee9149d5bf8b62025fdb45a83465f:token-indexer/test/fixtures/color-vectors.json:generic-api-key:29
4e06464af1cee9149d5bf8b62025fdb45a83465f:token-indexer/test/fixtures/color-vectors.json:generic-api-key:41
4e06464af1cee9149d5bf8b62025fdb45a83465f:token-indexer/test/fixtures/color-vectors.json:generic-api-key:53
4e06464af1cee9149d5bf8b62025fdb45a83465f:token-indexer/test/fixtures/color-vectors.json:generic-api-key:65
4e06464af1cee9149d5bf8b62025fdb45a83465f:token-indexer/test/fixtures/color-vectors.json:generic-api-key:66
4e06464af1cee9149d5bf8b62025fdb45a83465f:token-indexer/test/fixtures/color-vectors.json:generic-api-key:77
4e06464af1cee9149d5bf8b62025fdb45a83465f:token-indexer/test/fixtures/color-vectors.json:generic-api-key:78
4e06464af1cee9149d5bf8b62025fdb45a83465f:token-indexer/test/fixtures/scan/mint-utwBTC.raw.json:generic-api-key:26
4e06464af1cee9149d5bf8b62025fdb45a83465f:token-indexer/test/fixtures/scan/mint-utwUSDC.raw.json:generic-api-key:26
# Project 00023 (PR #21): the live-transfer fixtures recorded from Stagenet carry the PUBLIC 32-byte
# token colour of each unshielded output (`unshieldedCreatedOutputs[].tokenType`) — the same class
# of false positive as the colour vectors above. A colour is a commitment anyone can derive from the
# contract address and the domain separator; nothing here is a credential. Fingerprints are pinned
# per commit, so a re-recorded fixture still fails the gate until it is reviewed.
dfe1917f17b145fe21ff97752631552fe26c28c7:token-indexer/test/fixtures/activity/ucom-transfer.raw.json:generic-api-key:22
dfe1917f17b145fe21ff97752631552fe26c28c7:token-indexer/test/fixtures/activity/ucom-transfer.raw.json:generic-api-key:29
dfe1917f17b145fe21ff97752631552fe26c28c7:token-indexer/test/fixtures/activity/ucom-transfer.raw.json:generic-api-key:38
bd2fa466b4a30a590f77734179ad97d7db6c078c:token-indexer/test/fixtures/activity/deposit-toMap.raw.json:generic-api-key:28
